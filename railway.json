{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "NPM_CONFIG_PRODUCTION=false npm install && npx prisma generate && npm run build && npx tsc -p tsconfig.seed.json"
  },
  "deploy": {
    "startCommand": "(npx prisma migrate deploy && node dist/prisma/seed.js) & exec node dist/src/main",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
